export const config = {
    port: 3000,
    environmet: 'production',
    connectionString: 'mongodb://localhost/hotelDB',
    useDataBase: false //true: it will use the mongodb service.
                      //false: it will use the mock data
}